# 📋 Как получить DATABASE_URL из PostgreSQL на Render

## Пошаговая инструкция:

### Шаг 1: Откройте Render Dashboard
1. Зайдите на [render.com](https://render.com)
2. Войдите в свой аккаунт
3. Вы попадете на главную страницу Dashboard

### Шаг 2: Найдите вашу PostgreSQL базу данных
1. В левом меню или на главной странице найдите раздел **"Databases"** или **"PostgreSQL"**
2. Или вверху страницы нажмите **"Databases"** в навигации
3. Найдите вашу базу данных (обычно называется `analog-discord-db` или похожее)

### Шаг 3: Откройте базу данных
1. Нажмите на название вашей базы данных
2. Откроется страница с информацией о базе данных

### Шаг 4: Найдите Internal Database URL
1. На странице базы данных найдите раздел **"Connections"** или **"Connection Info"**
2. Там будут два URL:
   - **External Database URL** - НЕ используйте этот!
   - **Internal Database URL** - используйте этот!
3. Скопируйте **Internal Database URL**
   - Формат: `postgresql://user:password@host:5432/dbname`
   - Пример: `postgresql://analog_discord_user:abc123@dpg-xxxxx-a.frankfurt-postgres.render.com/analog_discord`

### Шаг 5: Добавьте в переменные окружения
1. Вернитесь к вашему Web Service `gcord`
2. Откройте **Environment**
3. Добавьте переменную:
   - **Key:** `DATABASE_URL`
   - **Value:** `[вставьте скопированный Internal Database URL]`
4. Нажмите **"Save Changes"**

## Если база данных еще не создана:

### Создание PostgreSQL базы данных:

1. На главной странице Render нажмите **"+ New"** → **"PostgreSQL"**
2. Заполните:
   - **Name:** `analog-discord-db` (или любое другое имя)
   - **Database:** `analog_discord` (или оставьте по умолчанию)
   - **User:** `analog_discord_user` (или оставьте по умолчанию)
   - **Region:** Выберите ближайший (например, Frankfurt)
   - **Plan:** Free (для начала)
3. Нажмите **"Create Database"**
4. Дождитесь создания (1-2 минуты)
5. После создания откройте базу и скопируйте **Internal Database URL**

## Важно:

- ✅ Используйте **Internal Database URL** (для сервисов внутри Render)
- ❌ НЕ используйте **External Database URL** (для внешних подключений)
- Internal URL работает только между сервисами Render
- External URL нужен для подключения с вашего компьютера

## Визуальные подсказки:

На странице базы данных вы увидите что-то вроде:
```
Connection Info
├── Internal Database URL: postgresql://... (используйте этот!)
└── External Database URL: postgresql://... (не используйте)
```

Или может быть написано:
```
Internal Connection String: postgresql://...
```

