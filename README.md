# Analog Discord - Чат приложение с аудио комнатами

Полнофункциональный аналог Discord с чатом в реальном времени, комнатами, сообщениями и **аудио комнатами** через WebRTC.

## 🚀 Возможности

- ✅ Регистрация и авторизация пользователей
- ✅ Создание и управление комнатами
- ✅ Чат в реальном времени через WebSocket
- ✅ **Аудио комнаты с WebRTC** 🎤
- ✅ История сообщений
- ✅ Управление микрофоном
- ✅ Современный UI в стиле Discord
- ✅ SQLite база данных
- ✅ Готово для деплоя на Render.com

## 📦 Установка

1. Установите зависимости:
```bash
npm install
```

2. Создайте тестового пользователя (опционально):
```bash
node server/scripts/createUser.js
```

Это создаст пользователя:
- Email: `gg@mail.ru`
- Password: `Qwerty123`

## 🎮 Запуск

### Локально:
```bash
npm start
```

Или для разработки с автоперезагрузкой:
```bash
npm run dev
```

Сервер запустится на `http://localhost:5000`

## 🎤 Аудио комнаты

1. Войдите в систему и выберите комнату
2. Нажмите кнопку **"🎤 Войти в аудио"** в заголовке комнаты
3. Разрешите доступ к микрофону
4. Начните общаться голосом!

**Функции:**
- Включение/выключение микрофона
- Список участников аудио комнаты
- Автоматическое подключение к другим участникам
- Peer-to-peer соединения через WebRTC

## 🌐 Деплой на Render.com

См. подробную инструкцию в файле `render-deploy.md`

**Быстрый старт:**
1. Загрузите код в Git репозиторий
2. Создайте Web Service на Render.com
3. Подключите репозиторий
4. Render автоматически задеплоит приложение

## 💻 Использование

1. Откройте браузер и перейдите на `http://localhost:5000` (локально) или ваш URL на Render
2. Войдите в систему или зарегистрируйтесь
3. Создайте комнату или выберите существующую
4. Начните общаться текстом или войдите в аудио комнату!

## 🏗️ Структура проекта

```
analog/
├── server/
│   ├── index.js              # Главный сервер с WebSocket
│   ├── database/
│   │   └── db.js            # База данных SQLite
│   ├── routes/
│   │   ├── auth.js          # Аутентификация
│   │   ├── rooms.js         # API комнат
│   │   └── messages.js      # API сообщений
│   ├── socket/
│   │   └── socketHandler.js # Обработка WebSocket и WebRTC signaling
│   └── scripts/
│       └── createUser.js    # Создание пользователя
├── public/
│   ├── index.html           # Главная страница
│   ├── app.js              # JavaScript клиент
│   ├── webrtc.js           # WebRTC логика для аудио
│   └── styles.css          # Стили
├── render.yaml             # Конфигурация для Render.com
└── package.json
```

## 🔧 Технологии

- **Backend:** Node.js, Express, Socket.IO
- **Database:** SQLite
- **Frontend:** Vanilla JavaScript, HTML, CSS
- **Real-time:** WebSocket через Socket.IO
- **Audio:** WebRTC для peer-to-peer аудио соединений
- **STUN:** Google STUN серверы для NAT traversal

## 📝 API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход

### Комнаты
- `GET /api/rooms/my-rooms?userId={id}` - Мои комнаты
- `POST /api/rooms` - Создать комнату
- `POST /api/rooms/:id/join` - Присоединиться к комнате
- `GET /api/rooms/:id` - Информация о комнате

### Сообщения
- `GET /api/messages/room/:roomId` - Получить сообщения комнаты

## 🎨 Особенности

- **Real-time чат** - сообщения появляются мгновенно
- **Аудио комнаты** - голосовое общение через WebRTC
- **История сообщений** - все сообщения сохраняются в базе
- **Множественные комнаты** - создавайте сколько угодно комнат
- **Красивый UI** - интерфейс в стиле Discord
- **Готово для продакшена** - настроено для деплоя на Render.com

## 📄 Лицензия

ISC
