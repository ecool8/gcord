# 🎮 Analog Discord - Полнофункциональный клон Discord

Масштабируемое приложение-аналог Discord с серверами, каналами, текстовым чатом и голосовыми/видео комнатами.

## ✨ Возможности

- ✅ **Серверы (Guilds)** - создание и управление серверами
- ✅ **Текстовые каналы** - общение в реальном времени
- ✅ **Голосовые/видео каналы** - WebRTC для аудио/видео связи
- ✅ **Real-time чат** - Socket.IO для мгновенных сообщений
- ✅ **Аутентификация** - регистрация и вход с JWT
- ✅ **Масштабируемая архитектура** - PostgreSQL + Sequelize ORM
- ✅ **React фронтенд** - современный UI в стиле Discord
- ✅ **Готово для деплоя** - настроено для Render.com

## 🏗️ Технологии

### Backend
- **Node.js + Express** - REST API сервер
- **Socket.IO** - WebSocket для real-time
- **PostgreSQL + Sequelize** - масштабируемая база данных
- **JWT** - аутентификация
- **WebRTC** - peer-to-peer аудио/видео

### Frontend
- **React** - современный UI фреймворк
- **Socket.IO Client** - real-time соединения
- **WebRTC API** - голосовые/видео каналы
- **Axios** - HTTP клиент

## 📦 Установка

### Локальная разработка

#### Вариант 1: С SQLite (быстрый старт, без установки PostgreSQL)

1. **Установите зависимости:**
   ```bash
   npm run install-all
   ```

2. **Создайте .env файл:**
   ```bash
   # Windows PowerShell
   $env:USE_SQLITE="true"
   ```

   Или создайте файл `.env` вручную:
   ```
   USE_SQLITE=true
   PORT=5000
   NODE_ENV=development
   JWT_SECRET=local-dev-secret-key
   CLIENT_URL=http://localhost:3000
   ```

3. **Запустите приложение:**
   ```bash
   npm run dev
   ```

   - Backend: http://localhost:5000
   - Frontend: http://localhost:3000 (откроется автоматически)

#### Вариант 2: С PostgreSQL (рекомендуется для продакшена)

1. **Установите PostgreSQL:**
   - Скачайте с [postgresql.org](https://www.postgresql.org/download/)
   - Создайте базу данных: `CREATE DATABASE analog_discord;`

2. **Установите зависимости:**
   ```bash
   npm run install-all
   ```

3. **Создайте .env файл:**
   ```
   PORT=5000
   NODE_ENV=development
   DB_NAME=analog_discord
   DB_USER=postgres
   DB_PASSWORD=postgres
   DB_HOST=localhost
   DB_PORT=5432
   JWT_SECRET=local-dev-secret-key
   CLIENT_URL=http://localhost:3000
   ```

4. **Запустите приложение:**
   ```bash
   npm run dev
   ```

## 🚀 Деплой на Render.com

### Быстрый старт:

1. **Загрузите код в GitHub**

2. **Создайте PostgreSQL базу данных на Render:**
   - New + → PostgreSQL
   - Скопируйте Internal Database URL

3. **Создайте Web Service:**
   - New + → Web Service
   - Подключите GitHub репозиторий
   - Настройки:
     - **Build Command:** `npm run install-all && npm run build`
     - **Start Command:** `npm start`
     - **Environment:** Node

4. **Добавьте переменные окружения:**
   ```
   NODE_ENV=production
   DATABASE_URL=[ваш Internal Database URL из шага 2]
   JWT_SECRET=[сгенерируйте случайную строку]
   CLIENT_URL=[ваш Render URL]
   ```

5. **Деплой готов!** 🎉

Подробная инструкция в `RENDER_DEPLOY_INSTRUCTIONS.md`

## 📁 Структура проекта

```
analog/
├── server/
│   ├── index.js              # Главный сервер
│   ├── database/
│   │   └── sequelize.js      # Sequelize конфигурация
│   ├── models/               # Sequelize модели
│   │   ├── User.js
│   │   ├── Server.js
│   │   ├── Channel.js
│   │   ├── Message.js
│   │   └── ServerMember.js
│   ├── routes/               # API роуты
│   │   ├── auth.js
│   │   ├── servers.js
│   │   ├── channels.js
│   │   ├── messages.js
│   │   └── users.js
│   └── socket/
│       └── socketHandler.js  # Socket.IO обработчик
├── client/                   # React приложение
│   ├── src/
│   │   ├── components/       # React компоненты
│   │   ├── services/         # API и Socket сервисы
│   │   └── App.js
│   └── public/
└── package.json
```

## 🔧 API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/auth/me` - Текущий пользователь

### Серверы
- `GET /api/servers/my-servers` - Мои серверы
- `POST /api/servers` - Создать сервер
- `GET /api/servers/:id` - Получить сервер
- `POST /api/servers/:id/join` - Присоединиться к серверу

### Каналы
- `GET /api/channels/server/:serverId` - Каналы сервера
- `POST /api/channels` - Создать канал
- `GET /api/channels/:id` - Получить канал

### Сообщения
- `GET /api/messages/channel/:channelId` - Сообщения канала
- `POST /api/messages` - Создать сообщение

## 🎯 План развития

- [ ] Видео каналы (WebRTC video)
- [ ] Файлы и изображения в сообщениях
- [ ] Роли и права доступа
- [ ] Уведомления
- [ ] Поиск по сообщениям
- [ ] Редактирование/удаление сообщений
- [ ] Эмодзи и реакции
- [ ] Прямые сообщения (DM)

## 📄 Лицензия

ISC
