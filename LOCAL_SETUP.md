# 🚀 Локальный запуск Analog Discord

## Быстрый старт (SQLite - без PostgreSQL)

### 1. Установите зависимости:
```bash
npm run install-all
```

### 2. Запустите с SQLite:
```powershell
# Windows PowerShell
$env:USE_SQLITE="true"
npm run dev
```

Или создайте файл `.env`:
```
USE_SQLITE=true
PORT=5000
NODE_ENV=development
JWT_SECRET=local-dev-secret-key-minimum-32-characters
CLIENT_URL=http://localhost:3000
```

### 3. Откройте браузер:
- Приложение автоматически откроется на http://localhost:3000
- Или откройте вручную: http://localhost:3000

## С PostgreSQL (для продакшена)

### 1. Установите PostgreSQL:
- Скачайте: https://www.postgresql.org/download/windows/
- Установите с настройками по умолчанию
- Запомните пароль для пользователя postgres

### 2. Создайте базу данных:
```sql
-- Откройте pgAdmin или psql
CREATE DATABASE analog_discord;
```

### 3. Настройте .env:
```
PORT=5000
NODE_ENV=development
DB_NAME=analog_discord
DB_USER=postgres
DB_PASSWORD=ваш_пароль_postgres
DB_HOST=localhost
DB_PORT=5432
JWT_SECRET=local-dev-secret-key-minimum-32-characters
CLIENT_URL=http://localhost:3000
```

### 4. Запустите:
```bash
npm run dev
```

## Использование

1. **Регистрация:**
   - Откройте http://localhost:3000
   - Нажмите "Зарегистрироваться"
   - Заполните форму

2. **Создание сервера:**
   - После входа нажмите "+" в боковой панели
   - Введите название сервера
   - Сервер создастся с каналами "general" (текст) и "voice" (голосовой)

3. **Общение:**
   - Выберите текстовый канал для чата
   - Выберите голосовой канал для аудио
   - Нажмите "Подключиться" в голосовом канале

## Устранение проблем

### Ошибка подключения к БД:
- Если используете SQLite: убедитесь, что `USE_SQLITE=true` в .env
- Если используете PostgreSQL: проверьте, что PostgreSQL запущен и настройки в .env правильные

### Порт занят:
- Измените PORT в .env на другой (например, 5001)
- Или остановите процесс, использующий порт 5000/3000

### React не запускается:
- Убедитесь, что все зависимости установлены: `cd client && npm install`
- Проверьте логи в консоли

## Структура при запуске:

- **Backend сервер:** http://localhost:5000
- **React клиент:** http://localhost:3000
- **API Health:** http://localhost:5000/api/health

Приложение готово к использованию! 🎉

