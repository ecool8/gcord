# 🔧 Переменные окружения для Render.com

## Обязательные переменные:

### 1. NODE_ENV
- **Key:** `NODE_ENV`
- **Value:** `production`
- **Описание:** Указывает, что приложение работает в production режиме

### 2. DATABASE_URL
- **Key:** `DATABASE_URL`
- **Value:** `[Internal Database URL из PostgreSQL]`
- **Описание:** Строка подключения к PostgreSQL базе данных
- **Как получить:**
  1. Откройте вашу PostgreSQL базу данных на Render
  2. Скопируйте **"Internal Database URL"** (НЕ External!)
  3. Формат: `postgresql://user:password@host:5432/dbname`

### 3. JWT_SECRET
- **Key:** `JWT_SECRET`
- **Value:** `[случайная строка минимум 32 символа]`
- **Описание:** Секретный ключ для JWT токенов
- **Как сгенерировать (PowerShell):**
  ```powershell
  -join ((65..90) + (97..122) + (48..57) | Get-Random -Count 40 | % {[char]$_})
  ```

### 4. CLIENT_URL
- **Key:** `CLIENT_URL`
- **Value:** `https://gcord.onrender.com`
- **Описание:** URL вашего приложения (для CORS и Socket.IO)

## Опциональные (для улучшения сборки):

### 5. CI
- **Key:** `CI`
- **Value:** `false`
- **Описание:** Отключает строгие проверки при сборке React

### 6. GENERATE_SOURCEMAP
- **Key:** `GENERATE_SOURCEMAP`
- **Value:** `false`
- **Описание:** Отключает генерацию source maps (экономит память и время)

## Как добавить:

1. В разделе **Environment** нажмите **"+ Add"**
2. Введите **Key** и **Value**
3. Нажмите **"Save Changes"**
4. Render автоматически перезапустит сервис

## Важно:

- Убедитесь, что используете **Internal Database URL**, а не External
- JWT_SECRET должен быть уникальным и безопасным
- После добавления всех переменных перезапустите деплой

