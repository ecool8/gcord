# 🚀 Быстрый деплой на Render.com

## 📋 Пошаговая инструкция (5 минут)

### Шаг 1: Создайте PostgreSQL базу данных

1. Зайдите на [render.com](https://render.com) и войдите
2. Нажмите **"New +"** → **"PostgreSQL"**
3. Заполните:
   - **Name:** `analog-discord-db`
   - **Database:** `analog_discord`
   - **Plan:** Free (для начала)
   - **Region:** Выберите ближайший (например, Frankfurt)
4. Нажмите **"Create Database"**
5. ⚠️ **ВАЖНО:** Скопируйте **"Internal Database URL"** (понадобится в шаге 3)

### Шаг 2: Создайте Web Service

1. Нажмите **"New +"** → **"Web Service"**
2. Подключите GitHub:
   - Если GitHub не подключен: **"Connect GitHub"** → разрешите доступ
   - Выберите репозиторий: **`ecool7/analog_dis`**
   - Выберите ветку: **`main`**

### Шаг 3: Настройте Web Service

#### Основные настройки:
- **Name:** `analog-discord`
- **Environment:** `Node`
- **Region:** Тот же, что и у базы данных
- **Branch:** `main`
- **Root Directory:** (оставьте пустым)

#### Build & Deploy:
- **Build Command:**
  ```
  npm run install-all && npm run build
  ```
- **Start Command:**
  ```
  npm start
  ```

#### Environment Variables (переменные окружения):

Нажмите **"Add Environment Variable"** и добавьте:

1. **NODE_ENV**
   - Key: `NODE_ENV`
   - Value: `production`

2. **DATABASE_URL** (из шага 1)
   - Key: `DATABASE_URL`
   - Value: `[вставьте Internal Database URL из шага 1]`
   - Пример: `postgresql://user:pass@host:5432/dbname`

3. **JWT_SECRET**
   - Key: `JWT_SECRET`
   - Value: `[сгенерируйте случайную строку минимум 32 символа]`
   
   Как сгенерировать (PowerShell):
   ```powershell
   -join ((65..90) + (97..122) + (48..57) | Get-Random -Count 40 | % {[char]$_})
   ```

4. **CLIENT_URL** (можно добавить после деплоя)
   - Key: `CLIENT_URL`
   - Value: `[оставьте пустым или добавьте URL после деплоя]`

### Шаг 4: Запустите деплой

1. Прокрутите вниз и нажмите **"Create Web Service"**
2. Render начнет процесс:
   - Установит зависимости (2-3 минуты)
   - Соберет React приложение (1-2 минуты)
   - Запустит сервер
3. Дождитесь завершения (обычно 5-10 минут для первого раза)
4. Когда увидите **"✅ Your service is live"** - готово!

### Шаг 5: Получите URL

После успешного деплоя:
- В верхней части страницы будет **URL**
- Например: `https://analog-discord.onrender.com`
- **Скопируйте этот URL**

### Шаг 6: Обновите CLIENT_URL (опционально)

1. Вернитесь в настройки сервиса
2. Откройте **"Environment"**
3. Обновите **CLIENT_URL** на ваш Render URL
4. Нажмите **"Save Changes"**
5. Render автоматически перезапустит сервис

## ✅ Проверка работы

1. Откройте ваш URL: `https://your-app.onrender.com`
2. Проверьте health endpoint: `https://your-app.onrender.com/api/health`
   - Должно показать: `{"status":"ok","version":"3.0.0"}`
3. Зарегистрируйтесь и создайте сервер
4. Создайте канал и отправьте сообщение
5. Попробуйте голосовой канал

## 🔧 Важные замечания

### Бесплатный план:
- **Спин-даун:** Приложение "засыпает" после 15 минут неактивности
- **Первый запрос:** После пробуждения может занять 30-60 секунд
- **Лимиты:** 750 часов в месяц (достаточно для MVP)

### База данных:
- PostgreSQL создается автоматически
- Таблицы создаются при первом запуске (через Sequelize sync)
- На бесплатном плане: 1GB хранилища

### WebSocket:
- Socket.IO работает через HTTPS на Render
- Голосовые/видео каналы используют WebRTC (peer-to-peer)
- STUN серверы Google (бесплатные)

## 🔄 Обновление приложения

После каждого push в GitHub:
1. Render автоматически обнаружит изменения
2. Запустит новый деплой
3. Обновит приложение

Или вручную:
1. Откройте сервис на Render
2. **"Manual Deploy"** → **"Deploy latest commit"**

## 🐛 Решение проблем

### Ошибка подключения к БД:
- Проверьте, что DATABASE_URL правильный
- Убедитесь, что используете **Internal Database URL** (не External)
- Проверьте, что база данных создана и запущена

### Ошибка сборки:
- Проверьте логи в разделе "Logs"
- Убедитесь, что все зависимости установлены
- Проверьте, что Node.js версия >= 18

### WebSocket не работает:
- Убедитесь, что используете HTTPS URL
- Проверьте логи на наличие ошибок Socket.IO
- Откройте консоль браузера (F12) для отладки

### Приложение не запускается:
- Проверьте логи в Render
- Убедитесь, что все переменные окружения установлены
- Проверьте, что порт не указан (Render назначит автоматически)

## 📊 Мониторинг

На странице сервиса доступно:
- **Logs** - логи в реальном времени
- **Metrics** - CPU, память, сеть
- **Events** - история деплоев

## 🎉 Готово!

Ваше приложение теперь доступно по адресу:
`https://your-app-name.onrender.com`

Поделитесь URL с командой и начинайте тестировать MVP!

## 📝 Следующие шаги после деплоя:

1. Протестируйте все функции
2. Создайте несколько серверов и каналов
3. Протестируйте голосовые каналы
4. Соберите feedback от команды
5. Планируйте следующие фичи (см. README.md)

