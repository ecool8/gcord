# 🔐 Настройка Secret File в Render

## Преимущества использования Secret File:

✅ Все переменные в одном месте  
✅ Легко обновлять  
✅ Безопасное хранение секретов  
✅ Можно версионировать (в Git как шаблон)  

## Шаг 1: Подготовьте .env файл

1. Откройте файл `.env.production` в проекте
2. Замените значения на реальные:
   - `DATABASE_URL` - ваш Internal Database URL из PostgreSQL
   - `JWT_SECRET` - случайная строка 32+ символов
   - `CLIENT_URL` - ваш Render URL (https://gcord.onrender.com)

3. Сохраните файл

## Шаг 2: Загрузите в Render

1. Откройте ваш Web Service `gcord` на Render
2. Перейдите в **Environment** (в левом меню)
3. Найдите раздел **"Secret Files"**
4. Нажмите **"+ Add"**
5. В поле **"Filename"** введите: `.env.production`
6. В поле **"Contents"** вставьте содержимое вашего `.env.production` файла
7. Нажмите **"Save Changes"**

## Шаг 3: Проверка

После сохранения:
- Render автоматически перезапустит сервис
- В логах должно появиться: `✅ Loaded environment from Render secret file`
- Приложение должно работать с переменными из secret file

## Структура .env.production:

```env
NODE_ENV=production
DATABASE_URL=postgresql://user:pass@host:5432/dbname
JWT_SECRET=your-secret-key-here
CLIENT_URL=https://gcord.onrender.com
CI=false
GENERATE_SOURCEMAP=false
```

## Важно:

- ⚠️ НЕ коммитьте реальный `.env.production` с секретами в Git!
- ✅ Коммитьте только шаблон `.env.production` (с placeholder значениями)
- ✅ Реальный файл загружайте только через Render Secret Files
- ✅ Secret Files доступны в `/etc/secrets/<filename>` во время сборки и выполнения

## Альтернатива: Использование переменных окружения

Если предпочитаете отдельные переменные:
- Можно использовать обычные Environment Variables
- Оба способа работают одинаково хорошо
- Secret File удобнее для большого количества переменных

## Обновление Secret File:

1. Откройте **Environment** → **Secret Files**
2. Найдите ваш `.env.production`
3. Нажмите на него для редактирования
4. Обновите значения
5. Сохраните - Render перезапустит сервис автоматически

