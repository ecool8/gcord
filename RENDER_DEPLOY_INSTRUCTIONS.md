# 📋 Пошаговая инструкция по деплою на Render.com

## ✅ Шаг 1: Подготовка (уже сделано)
- ✅ Код запушен в GitHub: https://github.com/ecool7/analog_dis.git

## 🚀 Шаг 2: Создание аккаунта на Render.com

1. Перейдите на [https://render.com](https://render.com)
2. Нажмите **"Get Started for Free"** или **"Sign Up"**
3. Зарегистрируйтесь через GitHub (рекомендуется) или email
4. Подтвердите email, если потребуется

## 🔗 Шаг 3: Подключение GitHub репозитория

1. После входа в Render, нажмите **"New +"** в верхнем правом углу
2. Выберите **"Web Service"**
3. Нажмите **"Connect account"** рядом с GitHub (если еще не подключен)
4. Разрешите доступ Render к вашим репозиториям
5. Найдите и выберите репозиторий **`ecool7/analog_dis`**

## ⚙️ Шаг 4: Настройка Web Service

Заполните следующие поля:

### Основные настройки:
- **Name:** `analog-discord` (или любое другое имя)
- **Region:** Выберите ближайший регион (например, Frankfurt, Ireland)
- **Branch:** `main` (или `master`, если у вас master)
- **Root Directory:** Оставьте пустым (по умолчанию корень проекта)

### Build & Deploy:
- **Environment:** `Node`
- **Build Command:** `npm install`
- **Start Command:** `npm start`

### Plan:
- Выберите **Free** (бесплатный план)

## 🔐 Шаг 5: Настройка переменных окружения

В разделе **"Environment Variables"** добавьте:

1. Нажмите **"Add Environment Variable"**

2. Добавьте следующие переменные:

   ```
   NODE_ENV = production
   ```

   ```
   PORT = 5000
   ```
   (или оставьте пустым - Render автоматически назначит порт)

   ```
   JWT_SECRET = ваш-случайный-секретный-ключ-минимум-32-символа
   ```
   (сгенерируйте случайную строку, например: `openssl rand -hex 32`)

### Как сгенерировать JWT_SECRET:
- Windows PowerShell:
  ```powershell
  -join ((65..90) + (97..122) + (48..57) | Get-Random -Count 32 | % {[char]$_})
  ```
- Или используйте онлайн генератор: https://www.random.org/strings/

## 🚀 Шаг 6: Запуск деплоя

1. Прокрутите вниз и нажмите **"Create Web Service"**
2. Render начнет процесс деплоя:
   - Установит зависимости (`npm install`)
   - Запустит сервер (`npm start`)
   - Назначит URL вашему приложению

3. Дождитесь завершения деплоя (обычно 2-5 минут)
   - Вы увидите прогресс в логах
   - Когда увидите `✅ Your service is live`, деплой завершен!

## 🌐 Шаг 7: Получение URL

После успешного деплоя:

1. В верхней части страницы сервиса вы увидите **URL**
   - Например: `https://analog-discord.onrender.com`
   - Или: `https://analog-discord-xxxx.onrender.com`

2. **Скопируйте этот URL** - это адрес вашего приложения!

## ✅ Шаг 8: Проверка работы

1. Откройте ваш URL в браузере
   - Например: `https://analog-discord.onrender.com`

2. Проверьте health endpoint:
   - Откройте: `https://your-app.onrender.com/api/health`
   - Должно показать: `{"status":"ok","version":"2.0.0"}`

3. Попробуйте зарегистрироваться и создать комнату

## ⚠️ Важные замечания

### Бесплатный план Render:
- **Спин-даун:** Приложение "засыпает" после 15 минут неактивности
- **Первый запуск:** После спин-дауна первый запрос может занять 30-60 секунд
- **Лимиты:** 750 часов в месяц (достаточно для тестирования)

### База данных:
- SQLite файл создается автоматически на сервере
- Данные сохраняются между перезапусками
- Для продакшена рекомендуется PostgreSQL (платный план)

### WebSocket:
- Socket.IO автоматически работает через HTTPS на Render
- Аудио комнаты должны работать корректно

## 🔄 Обновление приложения

После каждого push в GitHub:
1. Render автоматически обнаружит изменения
2. Запустит новый деплой
3. Обновит приложение (обычно 2-5 минут)

Или вручную:
1. Откройте ваш сервис на Render
2. Нажмите **"Manual Deploy"** → **"Deploy latest commit"**

## 📊 Мониторинг

На странице сервиса вы можете:
- Видеть логи в реальном времени
- Проверять метрики (CPU, память)
- Просматривать историю деплоев

## 🐛 Решение проблем

### Приложение не запускается:
1. Проверьте логи в разделе "Logs"
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что `package.json` содержит правильный `start` скрипт

### Ошибка подключения:
1. Убедитесь, что сервис запущен (зеленый индикатор)
2. Проверьте URL - он должен начинаться с `https://`
3. Подождите 30-60 секунд после первого запуска (если было спин-даун)

### WebSocket не работает:
1. Убедитесь, что используете HTTPS URL
2. Проверьте логи на наличие ошибок Socket.IO
3. Откройте консоль браузера (F12) для отладки

## 📝 Чек-лист перед деплоем

- [x] Код запушен в GitHub
- [ ] Создан аккаунт на Render.com
- [ ] Подключен GitHub репозиторий
- [ ] Настроен Web Service
- [ ] Добавлены переменные окружения (NODE_ENV, JWT_SECRET)
- [ ] Запущен деплой
- [ ] Проверен health endpoint
- [ ] Протестировано приложение

## 🎉 Готово!

После выполнения всех шагов ваше приложение будет доступно по URL вида:
`https://your-app-name.onrender.com`

Поделитесь этим URL с друзьями, чтобы они могли присоединиться к вашим комнатам!

